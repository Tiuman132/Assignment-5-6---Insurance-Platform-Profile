services:
  nginx:
    build:
      context: ./infrastructure/nginx
      dockerfile: Dockerfile
    container_name: insurance-nginx
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - frontend-web
      - backend-api
    networks:
      - insurance-net

  frontend-web:
    build:
      context: ./frontend-web
      dockerfile: Dockerfile
    container_name: insurance-frontend
    working_dir: /app
    environment:
      NODE_ENV: development
      PORT: 3000
      NEXT_PUBLIC_API_BASE_URL: http://localhost/api
      CHOKIDAR_USEPOLLING: "true"
      WATCHPACK_POLLING: "true"
    volumes:
      - ./frontend-web:/app
      - frontend_node_modules:/app/node_modules
    expose:
      - "3000"
    depends_on:
      - backend-api
    networks:
      - insurance-net

  backend-api:
    build:
      context: ./backend-api
      dockerfile: Dockerfile
    container_name: insurance-backend
    working_dir: /app
    environment:
      NODE_ENV: development
      PORT: 5000
      MONGODB_URI: mongodb://mongodb:27017/insurance_platform
      JWT_SECRET: replace_with_strong_secret
      JWT_EXPIRES_IN: 2h
      FRONTEND_URL: http://localhost
      HTTPS_PFX_PATH: ./cert/server.pfx
      HTTPS_PFX_PASSPHRASE: replace_with_pfx_password
      KEYCLOAK_BASE_URL: http://keycloak:8080
      KEYCLOAK_REALM: insurance-platform
      KEYCLOAK_CLIENT_ID: backend-api
      KEYCLOAK_CLIENT_SECRET: replace_with_keycloak_client_secret
      KEYCLOAK_BACKEND_CALLBACK_URL: https://localhost/api/auth/keycloak/callback
      KEYCLOAK_FRONTEND_SUCCESS_REDIRECT_URL: https://localhost/auth/keycloak/success
      KEYCLOAK_FRONTEND_FAILURE_REDIRECT_URL: https://localhost/login?error=keycloak
      GENAI_API_BASE_URL: http://genai-api:11434
      OLLAMA_MODEL: mistral
    volumes:
      - ./backend-api:/app
      - backend_node_modules:/app/node_modules
    expose:
      - "5000"
    depends_on:
      - mongodb
      - keycloak
      - genai-api
    networks:
      - insurance-net

  mongodb:
    image: mongo:7.0
    container_name: insurance-mongodb
    restart: unless-stopped
    environment:
      MONGO_INITDB_DATABASE: insurance_platform
    ports:
      - "27017:27017"
    volumes:
      - mongodb_data:/data/db
    networks:
      - insurance-net

  keycloak:
    build:
      context: ./infrastructure/keycloak
      dockerfile: Dockerfile
    container_name: insurance-keycloak
    environment:
      KEYCLOAK_ADMIN: admin
      KEYCLOAK_ADMIN_PASSWORD: admin123
      KC_HTTP_ENABLED: "true"
      KC_PROXY_HEADERS: xforwarded
      KC_HOSTNAME_STRICT: "false"
    expose:
      - "8080"
    networks:
      - insurance-net

  genai-api:
    build:
      context: ./infrastructure/genai-api
      dockerfile: Dockerfile
    container_name: insurance-ollama
    environment:
      OLLAMA_MODEL: mistral
    expose:
      - "11434"
    volumes:
      - ollama_data:/root/.ollama
    networks:
      - insurance-net

  jenkins:
    build:
      context: ./infrastructure/jenkins
      dockerfile: Dockerfile
    container_name: insurance-jenkins
    ports:
      - "8081:8080"
    volumes:
      - jenkins_home:/var/jenkins_home
      - /var/run/docker.sock:/var/run/docker.sock
    networks:
      - insurance-net

volumes:
  mongodb_data:
  ollama_data:
  jenkins_home:
  frontend_node_modules:
  backend_node_modules:

networks:
  insurance-net:
    driver: bridge